#include <iostream>
#include <array>
#include <algorithm>

typedef char pesho[5];

pesho gosho = "";

static constexpr const char * const CSDI = "";



void get_finger_mode(bool *mode) {
    mode[0] = false;
}

#if 0
int main() {
    bool arr[28] {false};
    get_finger_mode(arr);

    pesho ex {*CSDI};
    std::cout << ex;
}
#endif

